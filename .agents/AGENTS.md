# Project Rules & Constraints

- **Backend (BE) Read-Only Policy**: DO NOT modify any Backend (BE) code or files under any circumstances. You may only read BE code to inspect schemas, endpoints, DTOs, and business logic.
- **Frontend (FE) Adaptation**: Take the Backend as the source of truth / ground truth. Modify ONLY the Frontend (FE) code to integrate smoothly and correctly with the Backend.
